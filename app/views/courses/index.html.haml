%h1 Courses

- @courses.each do |f|
	= f.name
	= f.description
	= link_to "Edit", edit_course_path(f)
	= link_to "Delete", course_path(f), method: :delete
	%br
	%br

%br
= link_to "Home", root_path
= link_to "Add Course", new_course_path